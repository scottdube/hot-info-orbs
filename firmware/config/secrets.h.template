#ifndef SECRETS_H
#define SECRETS_H

// ============= YOUR OWN CREDENTIALS — NEVER COMMIT THIS FILE =============
//
// Copy this file to  firmware/config/secrets.h  and fill in your own values.
// secrets.h is gitignored. The template you are reading is not, so it must
// stay empty.
//
// Both API keys below are FREE and take about a minute to register. Use your
// OWN keys: a key shared across several devices burns one free-tier quota and
// gets rate-limited, and whoever owns it can revoke it out from under you.

// --- REQUIRED ---------------------------------------------------------------

// Weather data.  Register: https://www.visualcrossing.com/sign-up
#define WEATHER_API_KEY ""

// Timezone / DST offsets.  Register: https://timezonedb.com/register
#define TIMEZONE_API_KEY ""

// --- OPTIONAL ---------------------------------------------------------------

// WiFi is normally configured on the device itself through the setup portal.
// Uncomment these two only as a fallback if the portal will not work for you.
//#define WIFI_SSID "MyWiFiRouter" // 2.4 GHz network only
//#define WIFI_PASS "WiFiPassword"

// MQTT broker credentials. Leave commented out if your broker needs no auth.
// The host, port and topic are NOT secret and live in config.h.
//#define MQTT_WIDGET_USER ""
//#define MQTT_WIDGET_PASS ""

// Custom web-data widget endpoints. These live here rather than in config.h
// because a data URL routinely carries an API key in its query string.
//#define WEB_DATA_WIDGET_URL ""
//#define WEB_DATA_STOCK_WIDGET_URL "http://<host>/stocks.php?stocks=SPY,VT,GOOG"

#endif
