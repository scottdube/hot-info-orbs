# Name,   Type, SubType, Offset,  Size, Flags
# Two app slots for OTA (docs/REQUIREMENTS.md R2). An update writes the idle
# slot and switches only once the image is complete, so an interrupted upload
# keeps the old firmware. The app was 1.60 MB when this was laid out
# (2026-09-23), ~83% of a slot. The filesystem shrank from 1.9 MB to 128 KB: only
# WebDataWidget's image cache uses it, and nothing calls LittleFS.begin().
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x1E0000,
app1,     app,  ota_1,   0x1F0000,0x1E0000,
spiffs,   data, spiffs,  0x3D0000,0x20000,
coredump, data, coredump,0x3F0000,0x10000,
